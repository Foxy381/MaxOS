# MaxOS
MaxOS is a Python-based OS with a GUI and command line, blending features of Windows, macOS and LightOS. It operates via windows and commands. Features include help, system info, calculator, and file folders. In older versions, playing music requires a reboot to stop. Shut down using Sleep or Exit command.
